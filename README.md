# Hahamera

Simple Fabric mod for Minecraft 1.21.1 adding the Tomato block.

## Build

The GitHub Actions workflow builds the mod automatically on pushes to `main` and uploads the JAR as a workflow artifact.
